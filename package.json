{
	"license": "AGPL-3.0",
	"type": "module",
	"scripts": {
		"dev": "pnpm --filter @bgs/api --filter game --filter @bgs/web --filter @bgs/docs run --parallel dev",
		"dev:admin": "pnpm --filter @bgs/admin run dev",
		"format": "oxfmt .",
		"format-check": "oxfmt --check ."
	},
	"devDependencies": {
		"@typescript/native-preview": "7.0.0-dev.20260707.2",
		"oxfmt": "catalog:",
		"svelte-language-server": "^0.18.3",
		"typescript": "catalog:"
	},
	"engines": {
		"node": ">=24"
	},
	"packageManager": "pnpm@11.11.0",
	"_pnpmOverridesNote": "Overrides are now in pnpm-workspace.yaml (pnpm 11+ moved settings from package.json)."
}
